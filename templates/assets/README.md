# DashForge Asset Packs

Put downloadable dashboard image packs in this folder.

Each pack should be referenced from `index.json` and can contain PNG, JPG, HEIC
or WebP files. DashForge installs those files into the local `DashboardAssets`
library, where they can be used by image widgets, stickers and page backgrounds.

